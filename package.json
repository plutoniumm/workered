{
  "name": "workered",
  "version": "1.0.0",
  "description": "Run a module in a Web Worker. And push custom functions",
  "source": "index.js",
  "repository": "plutoniumm/workered",
  "type": "module",
  "scripts": {
    "dev": "rollup -c -w",
    "build": "rollup -c",
    "git": "sh ./scripts/git.sh",
    "predeploy": "npm run build",
    "deploy": "npm t && git commit -am $npm_package_version && git tag $npm_package_version && git push && git push --tags && npm publish"
  },
  "license": "MIT",
  "dependencies": {
    "express": "4.18.1",
    "rollup": "2.75.7",
    "rollup-plugin-terser": "7.0.2",
    "@rollup/plugin-commonjs": "22.0.1",
    "@rollup/plugin-node-resolve": "13.3.0"
  }
}